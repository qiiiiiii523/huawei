experiment_arm: C
experiment_variant: C1
status: runnable_after_rpeak_manifest_validation
references: {common: configs/common.yaml, training: configs/training_protocol_v1.yaml, preprocessing: configs/preprocessing.yaml, losses: configs/losses.yaml, strict_index: metadata/d12_strict_pretrain_index.csv, rpeak: configs/rpeak_pseudopair.yaml}
task_id: task1
visible_leads: [I]
pseudo_data:
  derived_dir: "../task1_rpeak_pseudo_output"
  selection: top_similarity_quantile
  similarity_quantile: 0.75
  expected_windows: 18
stages:
  - name: strict_d12_i_pretrain
    supervision_mode: d12_i_pretrain
    alignment_mode: same_window
    pointwise_loss_allowed: true
    loss_profile: strict_sync
  - name: rpeak_pseudo_finetune
    supervision_mode: rpeak_pseudo_adaptation
    alignment_mode: pseudo_rpeak_monotonic_warp
    physical_sync: false
    pseudo_pointwise_loss_allowed: true
    required_pair_status: accepted
    loss_profile: rpeak_pseudo
