experiment_family: B1_T1_A
task_id: task1
route: A
network: LightweightUNet
input_channels: 1
output_channels: 12
seed: 42
deterministic: true
batch_size: 16
epochs: 100
num_workers: 0
torch_threads: 8
optimizer: AdamW
learning_rate: 0.001
weight_decay: 0.0001
baseline_head: disabled
adapter: disabled
raw_baseline_policy: zero_model_baseline
loss:
  observed_consistency: 0.10
  spectral_stat: 0.20
  physiology: 0.05
  pair_invariant_stat_L1: 0.20
evaluation:
  official_view: raw_uV
  checkpoint_selection: best_validation_task1_r1
  copy_at_eval: replace_observed_leads_from_input
